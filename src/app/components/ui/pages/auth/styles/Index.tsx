import { Box, styled } from '@mui/material';

export const AuthPageContainer = styled(Box)(() => ({
    height: '100vh',
    position: 'relative',
    backgroundColor: '#F6FEF6',
    overflow: 'hidden',
    padding: '25px',
}));
